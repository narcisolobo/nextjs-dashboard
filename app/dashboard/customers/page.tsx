import { Metadata } from 'next';

const metadata: Metadata = {
  title: 'Customers',
};

function CustomersPage() {
  return (
    <div>
      <p>CustomersPage</p>
    </div>
  );
}

export { metadata };
export default CustomersPage;
